{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm -F docs-site build",
  "outputDirectory": "docs-site/build",
  "framework": "docusaurus",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
