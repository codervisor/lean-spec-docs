---
id: index
title: 什么是 LeanSpec？
sidebar_label: 概述
sidebar_position: 1
description: 为 AI 驱动的开发设计的轻量级规范驱动开发方法论
---

# 什么是 LeanSpec？

LeanSpec 是一种为 AI 时代设计的轻量级**规范驱动开发 (SDD)** 方法论。它帮助团队——包括人类和 AI 代理——专注于*意图*而非实现细节。

> **可以把它看作是"项目管理的 Markdown"**：简单、结构化且机器可读。

## 为什么选择 LeanSpec？

传统的规范往往过于冗长（没人读）或过于模糊（AI 会产生幻觉）。LeanSpec 填补了这一空白：

1.  **上下文经济 (Context Economy)**：规范保持精简（&lt;2,000 tokens），以适应 AI 上下文窗口。
2.  **活的文档**：规范随代码演进，通过状态跟踪（`planned` → `in-progress` → `complete`）。
3.  **AI 原生**：结构化的 frontmatter 和清晰的章节帮助 AI 代理理解*构建什么*以及*为什么*。

## 您将获得什么

*   **核心 CLI**：管理规范、跟踪状态并可视化依赖关系（`lean-spec board`, `lean-spec deps`）。
*   **MCP 服务器**：让 AI 代理（Claude, Cursor 等）直接读取和搜索您的规范。
*   **可视化模式**：一个精美的 Web 界面，用于浏览项目的规范图谱（`lean-spec ui`）。
*   **模板**：内置的功能、重构和实验模式。

## 一个简单的示例

LeanSpec 只是一个带有 frontmatter 的 Markdown 文件：

```markdown
---
status: in-progress
created: 2025-11-07
priority: high
tags: [api, auth]
---

# 用户认证 API

## 目标
为移动应用启用安全的用户登录和会话管理。

## 关键场景
1. 用户使用电子邮件/密码登录 → 接收 JWT 令牌
2. 用户使用令牌访问受保护的端点 → 获取数据
3. 用户令牌过期 → 接收 401，必须重新认证

## 验收标准
- [ ] 登录端点在成功时返回有效的 JWT
- [ ] JWT 包含用户 ID 和过期时间
- [ ] 受保护的端点验证 JWT 签名

## 非目标
- 社交登录（未来功能）
- 密码重置（单独的规范）
```

## 下一步

*   **[快速开始](/docs/guide/getting-started)**：安装 CLI 并创建您的第一个规范。
*   **[理解 LeanSpec](/docs/guide/understanding-leanspec)**：深入了解"上下文经济"哲学。
*   **[AI 集成](/docs/guide/usage/ai-assisted/mcp-integration)**：将 LeanSpec 连接到您的 AI 编码助手。
