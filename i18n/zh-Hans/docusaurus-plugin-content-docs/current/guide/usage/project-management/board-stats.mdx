---
id: 'board-stats'
title: '看板与统计'
sidebar_position: 10
---
# 看板与统计

使用 LeanSpec 的看板和统计命令即时了解项目的健康状况。

## 看板命令

您的规范的可视化看板视图：

```bash
lean-spec board
```

显示按状态列组织的规范：
- 📋 **计划中** - 准备开始的规范
- 🚧 **进行中** - 活跃工作
- ✅ **已完成** - 已完成的规范

### 项目完成度摘要

看板包括项目完成度摘要：
- 总规范数
- 按状态分布（计划中、进行中、已完成）
- 完成百分比
- 进行中的活跃工作

### 过滤

过滤看板视图：

```bash
# 仅显示高优先级规范
lean-spec board --priority high

# 显示带特定标签的规范
lean-spec board --tag feature

# 组合过滤器
lean-spec board --priority high --tag api
```

## 统计命令

快速项目指标：

```bash
lean-spec stats
```

显示：
- 总规范数
- 状态分解（计划中、进行中、已完成）
- 优先级分布
- 最常见的标签
- 最近活动摘要

### 完整分析

获取详细分析：

```bash
lean-spec stats --full
```

提供全面的项目洞察：
- 详细的状态指标
- 优先级分解
- 标签频率分析
- 完成趋势
- 每个状态的平均时间
- 健康指标

## 工作流程

### 每日站会

```bash
# 快速项目概述
lean-spec board

# 查看进行中的工作
lean-spec board --status in-progress

# 检查高优先级项目
lean-spec board --priority high
```

### 冲刺规划

```bash
# 审查项目健康状况
lean-spec stats --full

# 查看准备开始的工作
lean-spec board --status planned

# 检查容量
lean-spec stats
```

### 每周回顾

```bash
# 整体项目指标
lean-spec stats --full

# 审查已完成的工作
lean-spec board --status complete

# 识别瓶颈
lean-spec board --status in-progress
```

## 项目健康指标

看板和统计命令帮助识别：

### ⚠️ 警告标志
- 进行中的规范过多（焦点问题）
- 长时间没有更新的旧规范（停滞工作）
- 计划规范的百分比高（执行差距）
- 具有未满足依赖的规范（使用 `lean-spec deps` 检查）

### ✅ 健康信号
- 稳定的完成率
- 平衡的状态分布
- 活跃的最近更新
- 清晰的优先级焦点

## 最佳实践

1. **每天检查** - 以 `lean-spec board` 开始新的一天
2. **每周审查** - 使用 `lean-spec stats --full` 查看趋势
3. **限制在制品** - 控制进行中的规范
4. **定期更新** - 使用 `lean-spec update` 保持状态最新
5. **使用优先级** - 首先关注高优先级工作

## 提示

- 使用 `lean-spec board` 获取可视化概述
- 使用 `lean-spec stats` 获取快速指标
- 与 `lean-spec deps` 结合使用以了解依赖关系
- 导出统计数据用于报告（管道到文件或工具）
- 使用过滤聚焦于特定区域

## 示例输出

```
📊 LeanSpec 看板

📋 计划中 (3)
  • 045-new-feature [high] #feature
  • 046-refactor [medium] #tech-debt
  • 047-docs-update [low] #documentation

🚧 进行中 (2)
  • 043-launch [high] #release
  • 044-api-update [high] #feature

项目完成度：71%（12/17 个规范完成）
- 2 个进行中，3 个计划中，12 个已完成
```

## 了解更多

- **[依赖关系](/zh-Hans/docs/guide/usage/project-management/dependencies)** - 跟踪规范关系
- **[验证](/zh-Hans/docs/guide/usage/project-management/validation)** - 检查规范质量
- **[CLI 参考](/zh-Hans/docs/reference/cli)** - 完整命令列表
