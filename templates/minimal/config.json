{
  "name": "Minimal",
  "description": "Just the essentials - folder structure only",
  "config": {
    "template": "minimal",
    "specsDir": "specs",
    "structure": {
      "pattern": "flat",
      "prefix": "",
      "dateFormat": "YYYYMMDD",
      "sequenceDigits": 3,
      "defaultFile": "README.md"
    }
  }
}
